# Batch 74 phase-2 notes (API diagnosis)

## Done
- [x] UI fixes complete (picker scrollbar hidden, picker centered left-1/2 -translate-x-1/2, chat column centering justify-center items-center w-full). Typecheck clean. 109 tests pass (1 skipped).
- Checkpoint NOT yet saved for batch 74 UI fixes.

## Remaining: NVIDIA model no-response diagnosis
User: "Bro ye error hai kya?" screenshot (11:01) — user sent a screenshot of GitHub + "Bro ye error hai kya?"; assistant bubble shows cursor (streaming) but never responded; composer send button shows stop icon (square) = streaming stuck forever. Model: MiniMax M3 (Nvidia provider). User says he selected Nvidia model, no response, streaming hangs.

### Hypotheses
1. Streaming SSE never ends (response finished but client never closes) → stop button stuck, cursor blink forever.
2. Nvidia upstream timeout / model retired / rate limit → no error surfaced.
3. Provider key not set → but guard should show error bubble.

### Where to look
- server/aiProxy.ts: proxy streaming for nvidia (base url), timeout config, error surfacing.
- src/ai.ts: SSE parser, error handling.
- src/providers.ts: Nvidia model ids (MiniMax M3 = minimax-m1? check).
- Check if any model id changed upstream (Nvidia API catalog changed before, e.g., MiniMax may have been removed).

### Test method (used before)
curl -X POST http://127.0.0.1:3000/api/chat with {apiKey, provider, model, messages} -- streaming; also tests/nvidia-stream.test.ts exists (vitest, skips on 429). Run E2E via local prod server :3000. Note: dev server :3000 may be conflicted (EADDRINUSE 3001 in logs — actually :3001 error = a second instance tried). Kill stale: pgrep -f node, check .manus-logs/devserver.log.
Production server PID: pgrep -f "dist/index.js"; logs at .manus-logs/devserver.log.

### API keys available for tests (user's keys, stored in provider keys on device; for server tests use user's real keys shared in chat previously):
- Nvidia: NVAPI_PLACEHOLDER
- Mistral: MISTRAL_PLACEHOLDER
- Groq: GROQ_PLACEHOLDER
- OpenRouter: OPENROUTER_PLACEHOLDER
- OpenCode Zen: OPENCODE_PLACEHOLDER_1
(Nvidia key may be expired/daily-limited; previously 429 = account quota.)

### Sync after fix
- cd /home/ubuntu/ai_chat_app && python3 scripts/sync-full-via-api.py (Git Data API sync script; works; handles deletions)
- Checkpoint via webdev_save_checkpoint after tests pass.
- Live link: https://aichatapp-8ksusdph.manus.space

## User's 3rd screenshot issues (11:01)
- Streaming cursor stuck forever, no reply, stop icon in composer.
- Model selected: MiniMax M3 (Nvidia).
- Also message bubble alignment (fixed in phase 1).

## aiProxy.ts findings (read 2026-08-19)
- server/aiProxy.ts: handles /api/chat and /api/ai-proxy/chat. Provider bases: nvidia=integrate.api.nvidia.com/v1, mistral=api.mistral.ai/v1, groq=api.groq.com/openai/v1, openrouter=openrouter.ai/api/v1, opencode_zen/opencode=opencode.ai/zen/v1, gemini=generativelanguage.googleapis.com/v1beta. TIMEOUT_MS=60s.
- streamRelay bug: `const timer = setTimeout(() => res.status(504).end(), TIMEOUT_MS)` is only used on upstream fetch; the SSE pump loop has NO idle-timeout — if upstream stops sending mid-stream (e.g., model finishes without final [DONE], or network stall), client never knows → cursor blinks forever, stop button stuck. ALSO timer cleared right after fetch, so if upstream takes >60s to start responding it 504s but during stream no watchdog.
- Client disconnect abort: pump never checks client close — if user closes/navigates, keeps reading forever. res.on("close") missing.
- Fix plan: add stream watchdog — if no data for e.g. 30s, end response; add res.on("close") to cancel reader; also on upstream end, ensure res.end always called (already done).
- Also check client side src/ai.ts: SSE reader abort handling (AbortController on stop btn) — check if stop sends abort to AbortSignal; client should close stream on stop.

## Next steps
1. Read src/ai.ts streamChat; check abort propagation to fetch("/api/chat", { signal }).
2. Add watchdog + close handler in streamRelay.
3. E2E test: curl -X POST 127.0.0.1:3000/api/chat -H 'Content-Type: application/json' -d '{"providerKey":"nvidia","apiKey":"NVAPI_PLACEHOLDER","modelId":"nvidia/llama-3.1-8b-instruct","body":{"messages":[{"role":"user","content":"Say OK"}],"stream":true}}' and watch stream end.
4. Test mistral/groq/openrouter/opencode too (same curl with keys).
5. Checkpoint, sync (scripts/sync-full-via-api.py), deliver.

## ai.ts findings
- src/ai.ts: streamChat fetches /api/chat with abort signal; client HAS inactivity timer (90s) on reader — resets per chunk. If upstream stream hangs mid-stream with NO final [DONE] and chunks still arriving periodically but client never sees [DONE]... timer only fires on dead connection.
- REAL HANG CAUSE: server streamRelay has NO inactivity watchdog during pump — if upstream stalls mid-stream for >60s-90s total, client timer kills (90s inactivity) → but user sees "took too long" OR nothing if chunks arrived.
- ALSO: if upstream model replies without sending [DONE] sentinel (some providers end SSE without final chunk), client loop breaks, started=true, onDone fires — that's fine.
- Most likely hang scenario: upstream response is non-streamed despite stream:true (e.g., Nvidia returns chunked then closes) — but client handles [DONE] fine.
- Strong candidate: server-side `res.status(504).end()` timer cleared after fetch; during stream no watchdog; if upstream body stalls (e.g., Nvidia slow cold start >60s), client gets nothing and 90s timer fires "took too long". BUT user says "response nahi de raha" + blinking cursor = probably [DONE] never received and stream open with no chunks for a while, then client error OR just stuck cursor.
- User said also stop button not working earlier (batch). If abort signal kills fetch but server keeps relaying, that's fine. If server stream ends cleanly with [DONE] but UI doesn't finalize...
- FIX to apply in streamRelay (server):
  1. Add per-chunk inactivity watchdog in pump: if no data for STREAM_IDLE_MS (30s), end response cleanly.
  2. res.on("close") → reader.cancel() to stop pumping when client disconnects/aborts.
  3. Also clear res X-Request-Body header issue? (minor — sending huge header; fine).

## E2E diagnosis result (curl via dev server :3001)
- Nvidia returns 400 "model field is required (missing_required_field)" for modelIds z-ai/glm-5.2, minimaxai/minimax-m3, nvidia/llama-3.1-8b-instruct.
- aiProxy line ~72: `await streamRelay(res, url, "POST", providerKey, headers, body, { ...body, model: modelId })` — 6th arg overrides; but Nvidia upstream may require the model INSIDE the JSON body AND perhaps the body merge is wrong: if `body` already contains `model` from client, { ...body, model: modelId } should work... UNLESS the Nvidia route expects the model ALSO in `model` header or the body is passed but headers set Content-Type differently.
- REAL CAUSE candidate: streamRelay may stringify the extra argument INSIDE JSON body correctly, but if modelId contains slash e.g. "z-ai/glm-5.2", Nvidia API accepts it... 400 says model missing → maybe aiProxy for nvidia uses a DIFFERENT payload builder (gemini-style?) or modelId is not forwarded because Nvidia body uses `model: req.body.modelId` only when providerKey === "nvidia" special path?
- CHECK: server/aiProxy.ts lines 60-72 actual code + whether a body.model exists already (client sends `body` = {messages, stream} — no model field). So { ...body, model: modelId } SHOULD have model. Yet upstream says missing. → streamRelay might not send that 6th arg as body! Check streamRelay signature args order.
- Next: read streamRelay definition (lines ~150-220) to see arg mapping.

## UPDATE (post-compaction re-read, 2026-08-19)
- streamRelay signature IS correct: 6th arg `sentBody` goes as `JSON.stringify(body)` to upstream and X-Request-Body echo; body arg (5th) is the actual sent JSON — and handleChat passes `body` (client payload) as arg 5 and `{...body, model: modelId}` as arg 6. So upstream SHOULD receive model. BUT earlier curl got 400 missing_required_field.
- IMPORTANT: the earlier curl tests may have used wrong curl syntax (body: { messages, stream } WITHOUT model field in `body` key if I passed `body` key only). Verify fresh:
  curl -X POST http://127.0.0.1:3001/api/chat -d '{"providerKey":"nvidia","apiKey":"<NVKEY>","modelId":"z-ai/glm-5.2","body":{"messages":[{"role":"user","content":"Say OK"}],"stream":true}}'
- If 400 again: check whether body sent by handleChat loses model — possible cause: Express body-parser issue with nested keys or `body` being a reserved property? No — JSON.stringify(body) where body={messages,stream} + model=modelId should include model.
- ALSO: watch out for `stream: true` with max_tokens — earlier test used max_tokens 10/5 which is fine.
- Dev server: dev:server on 3000, :3001 conflicts seen — tsx watch restarts. Current port per project status: apiServer 3000.
- Next: fresh E2E test via :3000; then groq/mistral/openrouter/opencode with keys above.
- Keys: NVAPI_PLACEHOLDER (nvidia), MISTRAL_PLACEHOLDER (mistral), GROQ_PLACEHOLDER (groq), OPENROUTER_PLACEHOLDER (openrouter), OPENCODE_PLACEHOLDER_1 (opencode_zen).
- Note: Nvidia key may be expired/429 (was seen before). If 429, surface via friendly error — client already has rate-limit notice.
- Watchdog + res.on("close") fix already applied to streamRelay in a previous edit (verify it's present in current file at lines ~127-236 — IT IS present: idleTimer 30s watchdog + res.on("close") both in place).
- sync script: python3 scripts/sync-full-via-api.py ; checkpoint: webdev_save_checkpoint; live: https://aichatapp-8ksusdph.manus.space
