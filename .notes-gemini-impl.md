# Gemini client impl status (2026-08-19 06:00 UTC)

## Done
- src/providers.ts: ProviderKey now includes "gemini"; PROVIDER_LABELS has Gemini; PROVIDERS map has gemini entry (url https://generativelanguage.googleapis.com/v1beta, no bareId); MODELS has gemini/gemini-2.0-flash-lite + gemini/gemini-2.5-flash-preview-05-20 (both vision).
- src/ai.ts streamChat: gemini branch builds payload { providerKey, apiKey, modelId, gemini:{ modelId, body:{ contents:[{role:"model"|"user", parts:[{text}]}], generationConfig:{maxOutputTokens:2048, temperature, topP} } } } matching server's expected shape.

## Server side (server/index.ts) — already implemented earlier
- PROVIDERS.gemini exists; pickKey works for gemini (client sends apiKey in body; user saves key in settings.apiKeys.gemini — dynamic since SettingsModal iterates PROVIDER_META).
- /api/chat gemini branch: `${PROVIDERS.gemini.url}/models/${gemini.modelId}:streamGenerateContent?key=${encodeURIComponent(key)}&alt=sse`; upstreamBody = gemini.body. SSE pump relays raw upstream lines (Gemini alt=sse outputs `data: {"candidates":[...]}` JSON lines — client SSE parser in ai.ts parses JSON and reads ev.candidates[0].content.parts[0].text?? NO — client parser reads ev.choices[0].delta.content. GEMINI SSE FORMAT DIFFERS!

## CRITICAL remaining: SSE format mismatch
- Gemini streamGenerateContent alt=sse returns lines like: `data: {"candidates":[{"content":{"parts":[{"text":"..."}]}}]}` (candidates[].content.parts[].text).
- Client ai.ts parser: `ev.choices?.[0]?.delta?.content` → Gemini lines will never emit delta → "The model sent no content" error.
- FIX: in ai.ts SSE loop, after delta check, add gemini fallback:
  const cand = ev?.candidates?.[0]?.content?.parts?.[0]?.text;
  if (cand) cb.onDelta(cand);
  Gemini has no [DONE]; watchdog 90s handles end. Client on stream-end (done with started false) error "no content" — but watchdog res.end happens server-side; client sees done without started → "no content" error. Better: when stream ends without [DONE], treat started||streamEnded as done IF any delta arrived; if none → "no content" error is fine. ALSO server pump now injects "data: [DONE]" after stream end, so client onDone fires even for Gemini — good.
- testApiKey in ai.ts: gemini case missing — add providerKey === "gemini" → modelId "gemini-2.0-flash-lite", body = gemini shape with maxOutputTokens 5.
- SettingsModal PROVIDER_META (src/components/SettingsModal.tsx) lists providers with envHint/getApiUrl — need gemini entry: label "Gemini", envHint "Get from Google AI Studio", getApiUrl "https://aistudio.google.com/app/apikey". Find PROVIDER_META definition in SettingsModal.tsx or storage.

## Keys
- google studio (user): GEMINI_PLACEHOLDER
- test command: node scripts/probe-provider.mjs gemini gemini-2.0-flash-lite 3001 (probe already supports gemini via {providerKey, apiKey, modelId, gemini:{modelId,body}} payload).

## Progress update (06:00)
- DONE: SettingsModal PROVIDER_META has gemini entry (aistudio google apikey); ai.ts testApiKey supports gemini (non-stream); ai.ts SSE loop has Gemini candidates[].content.parts[].text fallback.
- Server already supports gemini via /api/chat gemini branch with [DONE] injection after stream.
- Gemini probe result: gemini-2.0-flash-lite → 404 "use gemini-3.5-flash-lite"; gemini-3.5-flash-lite → 403 PERMISSION_DENIED "Your project has been denied access"; gemini-2.5-flash/pro → 404 (retired). The user's Google key has its project denied — NOT an app bug. Gemini support code is complete and wired; it will work once user creates a fresh API key at aistudio.google.com/app/apikey and pastes it in Settings. Catalog kept at gemini-3.5-flash-lite (current recommended) + remove 2.5 preview (retired) → keep one model gemini/gemini-3.5-flash-lite (vision).

## FINAL STEPS STATE (06:01)
- Gemini catalog = gemini/gemini-3.5-flash-lite only (vision). testApiKey uses gemini-3.5-flash-lite. ai.ts SSE has candidates fallback. SettingsModal has Gemini API key field + Get API Key (aistudio.google.com/app/apikey).
- Gemini user key returns 403 PERMISSION_DENIED (project denied by Google) — code complete, works after user makes new key at aistudio.google.com.
- Tests fixed: remote-config.test.ts allows gemini entries; providers.test.ts expects 19 models/5 providers → NEED TO UPDATE to 20 models / 6 providers (still pending!).
- todo.md batch 74 appended; last pending: tests, checkpoint, github sync, deliver.

## After Gemini: final steps
- Update todo.md Batch 74 entries [x]; checkpoint (batch 74); python3 scripts/sync-full-via-api.py; deliver https://aichatapp-8ksusdph.manus.space.
- Replace minimaxai/minimax-m3 in MODELS? It is user's last-chosen model but Nvidia marks it DEGRADED upstream. Keep catalog (user may retry) but default is z-ai/glm-5.2 (works). Optionally swap to meta/llama-3.3-70b-instruct — DECISION: swap minimax entry to a working model? Keep; user picks manually; friendly error shows. Better: keep.
