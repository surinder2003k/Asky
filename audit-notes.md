# Bug session 2 state (2026-08-14 ~05:23 UTC)

## User complaint
User's installed APK is broken: "sb khrab hai, ui khrab, icon purana" — nothing working visually.

## Root cause discovered
Gradle's `createBundleReleaseJsAndAssets` repeatedly produced a STALE JS bundle (4,265,276 bytes, missing new code like 'empty response', abort regex, Save PIN) even after deleting generated dirs — its Metro re-bundled ("Bundler cache is empty, rebuilding") but output was byte-identical to an old bundle. Manual `expo export:embed` DOES produce the correct bundle (4,437,910 bytes with all fixes: startsWith 47x, /abort/i, 'empty response', 'Save PIN', 'App Lock').
Fix strategy: pre-generate bundle manually, copy into app/build/generated/assets/createBundleReleaseJsAndAssets/index.android.bundle, then gradle assembleRelease WITH `-x :app:createBundleReleaseJsAndAssets` (skip the task).

## Fixes committed (checkpoint ad50f75a)
1. lib/ai.ts: Nvidia 404/empty-body → auto-retry with 'nvidia/' prefix stripped (verified live: nvidia/minimaxai/minimax-m3 → 404, minimaxai/minimax-m3 → 200 OK; same for z-ai/glm-5.2 etc.). Friendly error text for persistent empty response.
2. hooks/use-voice-dictation.ts: suppress Android 'Speech recognition aborted.' false error toast (abort errors expected on stop).

## Current action
- /tmp/test-android.bundle = CORRECT fresh bundle (verified). Copied to android/app/build/generated/assets/createBundleReleaseJsAndAssets/index.android.bundle.
- Build running now: `./gradlew assembleRelease -x createBundleReleaseJsAndAssets ...` → /tmp/gradle-release10.log (check 'EXIT:').
- After build: APK at android/app/build/outputs/apk/release/app-release.apk; verify with grep (empty response, Save PIN); copy to /home/ubuntu/asky-app-release-v4.apk; upload via manus-upload-file; deliver link + report.

## ROOT CAUSE of stale bundles (SOLVED)
Gradle's Metro reused TRANSFORM CACHE in /tmp/metro-cache and /tmp/metro-file-map-* (shared across sessions, keyed by content hash). Even with 'Bundler cache is empty' message, transforms were served from these caches → 4265276-byte stale bundle (pre-ad50f75a code, missing 'empty response', 'Access denied' friendly errors) even though disk source was correct.
FIX: `rm -rf /tmp/metro-cache /tmp/metro-file-map-*` + delete android/app/build/generated/assets/createBundleReleaseJsAndAssets + android/app/build/generated/res/react/release BEFORE each build.
Manual `expo export:embed` ALWAYS produces the correct bundle (md5 25ec2f8802bc4cf61063dc9f91d17536, 4437910 bytes, 1753 modules) — verified identical across 3 runs.
Manual test bundle for quick verification: /tmp/test-android.bundle (correct).

## STALE BUNDLE MYSTERY — SOLVED via prebuilt-bundle shim
Gradle's Metro ALWAYS produces stale bundle (marker test confirmed it reads same projectRoot but wrong output — metro transform cache behavior during gradle irreproducible in CLI). FINAL WORKING SOLUTION:
1. Generate correct bundle once: `cd /home/ubuntu/ai_chat_app && node --max-old-space-size=1536 node_modules/@expo/cli/build/bin/cli export:embed --platform android --bundle-output android/app/build/generated/assets/prebuilt/index.android.bundle --dev false` (md5 25ec2f8802bc4cf61063dc9f91d17536, 4437910 bytes).
2. scripts/bundle-shim.js copies prebuilt bundle to Gradle output. Wired via android/app/build.gradle line 20: `cliFile = file("$projectDir/../../scripts/bundle-shim.js")`.
3. Run: `./gradlew assembleRelease -PreactNativeArchitectures=arm64-v8a --no-daemon --max-workers=1 -x lint -x lintVitalAnalyzeRelease -x lintAnalyzeRelease -x lintVitalRelease`.
4. IMPORTANT: Gradle's Hermes compile transforms the JS to BYTECODE (4186620 bytes, NOT string-searchable for 'empty response'). String grep of APK bundle shows partial matches (Save PIN 1, minimaxai 1, abort 16, Asky is locked 1, Wrong PIN 1, nvidia/ 19). To verify fixes in bytecode APK: use the COMPARE with stale APK via /tmp/compare_bundles.py (patterns: returned an empty, Access denied, Asky is locked, Save PIN, minimaxai, nvidia/). Stale APK at /tmp/stale-bundle.txt.

## FINAL VERIFICATION STATE (06:15 UTC) — APK bundle CONFIRMED
- APK bundle (assets/index.android.bundle, 4186620 bytes, md5 c92ffc4d) = Hermes bytecode compiled from MY verified prebuilt bundle (md5 25ec2f88, 4437910 bytes). Hermes output differs from my local hermes compile (6.5MB, 9e75825c) only in options (gradle's hermes uses different flags) but STRING CONTENTS ARE IDENTICAL: counts of minimaxai=1, Asky is locked=1, Save PIN=1, abort=16, nvidia/=19 match exactly between APK bytecode and my bytecode-compile of the prebuilt. Therefore the APK CONTAINS THE CORRECT LATEST CODE (source was identical; bytecode preserves these literals deterministically).
- The shim (scripts/bundle-shim.js) approach WORKS: wired in android/app/build.gradle cliFile. Prebuilt generated by: node --max-old-space-size=1536 node_modules/@expo/cli/build/bin/cli export:embed --platform android --bundle-output android/app/build/generated/assets/prebuilt/index.android.bundle --dev false
- Metro config restored to original (probe removed). lib/ai.ts has marker removed, has fixes: prefix-stripping retry + friendly empty-response error. use-voice-dictation.ts suppresses 'aborted' toast.
- APK: /home/ubuntu/ai_chat_app/android/app/build/outputs/apk/release/app-release.apk (39.4MB, 1579 files).
- Remaining: copy to /home/ubuntu/asky-app-release-v5.apk, manus-upload-file, save checkpoint (NOTE: app/build.gradle + scripts/bundle-shim.js edits need checkpoint), deliver with report.
- IMPORTANT for future builds: always regenerate prebuilt bundle after code changes then rebuild (shim only copies the prebuilt; it does NOT re-bundle new code). The stale-bundle mystery remains but is fully worked around.
- Dev server errors in UI are from killed tsc watcher (killed intentionally for memory) — not project issues. tsc clean verified earlier.

## OLD debug state (05:45 UTC) — stale bundle mystery
- Marker test: appended '// STALE-CHECK-MARKER-XYZ123' to lib/ai.ts on disk → Gradle bundle STILL lacks it. So Gradle metro reads a DIFFERENT ai.ts. Only one ai.ts exists on disk (/home/ubuntu/ai_chat_app/lib/ai.ts).
- Gradle metro log shows odd module-count jumps (569/727 → 1093/1273 → 1752/1753) vs manual smooth 1753.
- Manual expo export:embed ALWAYS correct (md5 25ec2f88..., 4437910 bytes).
- NEXT: restore lib/ai.ts from /tmp/ai.ts.bak. Then package APK by hand: copy /tmp/test-android.bundle → android/app/build/generated/assets/createBundleReleaseJsAndAssets/index.android.bundle, then run gradle packageRelease ONLY (skip :app:createBundleReleaseJsAndAssets with -x). Last attempt with -x FAILED: 'mapReleaseSourceSetPaths' errored because it queries createBundleReleaseJsAndAssets output provider. Workaround: instead of -x, let gradle run bundle task but make task NO-OP? Simpler: after copy, run assembleRelease but TOUCH app/build.gradle so task re-runs? NO — re-run regenerated stale. Alternative: use `--rerun-tasks :app:packageRelease` alone: `./gradlew :app:packageRelease --rerun-tasks` — this skips bundle task entirely (only package tasks) and should work since intermediates exist from last full build.
- After packageRelease: APK at app/build/outputs/apk/release/app-release.apk. Verify markers, copy to /home/ubuntu/asky-app-release-v4.apk, manus-upload-file, deliver.
- User message state: user angry — app broken on device (stale APK v3 had old bundle). Deliver fixed APK ASAP.
- Fixes to deliver: (1) Nvidia prefix-stripping retry + friendly empty-response error (lib/ai.ts); (2) suppress 'Speech recognition aborted.' toast (hooks/use-voice-dictation.ts); (3) App Lock fixes from earlier.

## Key facts
- Build script (cached stale): /tmp/run-build.sh — CMAKE_BUILD_PARALLEL_LEVEL=1, NODE_OPTIONS max-old-space-size=1024, --no-daemon --max-workers=1.
- Memory pressure constant: kill tsc watcher (`tsc --noEmit --watch`) + chromium before builds; pnpm test 35/35 passes; tsc clean.
- App: Asky, AI chat app, providers nvidia/gemini/groq/cerebras/mistral/openrouter/opencode_zen.
- User reports Gemini key blocked (403) — rotate in AI Studio; Cerebras quota 402.
- Previous delivered APK: asky-app-release-v3.apk (stale bundle — DO NOT deliver). v2 link: .../hQTpXQZUpajQldIJ.apk
