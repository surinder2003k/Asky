# Batch 76 notes (2026-08-19)

## Reel identification
- Instagram reel https://www.instagram.com/p/DcJNkt5TVb8 by @devzonex.dev (Aug 17, 2026)
- OG description: "DeepSeek V4 Pro Free ✅ ... Output Price: $0.0000. The model is now available through DeepSeek's app, website and API."
- Subject: **DeepSeek V4 Pro free API**

## Where is it free?
- OpenRouter API models list: NO free variant (deepseek/deepseek-v4-pro, -0813, -flash all cost money). "deepseek/deepseek-v4-pro-free" (zenmux) 404 on OpenRouter.
- Official DeepSeek API (platform.deepseek.com): V4 Pro is paid (off-peak $7/M in, $22/M out) — but "free" tier = daily free credits? Need to check.
- DeepSeek consumer chat is free (deepseek.com chat web) — some people wrap it.
- Check Nvidia build.nvidia.com: "deepseek-ai/deepseek-v4-pro" exists — build.nvidia offers free NIM credits for many models.
- Check Cerebras (cerebras.ai) — does not host deepseek.
- Most likely "free" path in reel: either DeepSeek official daily free credits on platform.deepseek.com, or openrouter free tier via zenmux (doesn't exist per 404), or the DeepSeek chat wrapper.

## TODO next
- Check build.nvidia.com deepseek-v4-pro (build.nvidia.com/deepseek-ai/deepseek-v4-pro exists per search) — likely the free source the reel points to, users can grab key from build.nvidia.com.
- Model id on Nvidia: deepseek-ai/deepseek-v4-pro

## Verified findings (2026-08-19)
- build.nvidia.com/deepseek-ai/deepseek-v4-pro: **Free Endpoint = Deprecated**. Only Partner Endpoint available now. Nvidia key WON'T work for free.
- OpenRouter: no free deepseek-v4-pro/-flash variant (zenmux "free" pages 404). Official OR prices: V4-Pro $1.32/$3.96 per M, V4-Flash $0.0765/$0.153 per M.
- Official DeepSeek API (api.deepseek.com): V4 Pro PAID ($1.32/$3.96 off-peak approx). No free API tier.
- The reel's "DeepSeek V4 Pro Free" = most likely refers to:
  1. DeepSeek consumer chat (chat.deepseek.com) free — unofficial reverse-engineered wrappers exist (Reddit "I reverse engineered DeepSeek Chat into a free OpenAI API")
  2. Possibly OpenCode Zen / another aggregator that lists deepseek-v4-pro-free
- Check OpenCode Zen (api.opencode.ai / sk-5yhn...) — user has OpenCode key, earlier tests passed "opencode" provider. zenmux.ai lists deepseek/deepseek-v4-pro-free → may be an OpenCode Zen model!
- DeepSeek official pricing page: deepseek-v4-pro and deepseek-v4-flash only, paid.

## Next action
- Probe OpenCode Zen API (user's key) for models containing "deepseek" → if deepseek-v4-pro exists there free, add it under opencode provider.
- If not found, add via OpenRouter if user opts in (user's OR key works). But reel said "FREE" — only plausible free source now: OpenCode Zen free tier or chat wrapper (not suitable).

## OpenCode Zen models probe (2026-08-19)
OpenCode Zen (https://opencode.ai/zen/v1) with user's key exposes 62 models, including: deepseek-v4-pro, deepseek-v4-flash, deepseek-v4-flash-free. The reel's "DeepSeek V4 Pro Free" free path = OpenCode Zen's deepseek-v4-flash-free (likely what the reel means; "Pro" also listed but flash-free is the free one). Next: E2E test deepseek-v4-flash-free (and deepseek-v4-pro) via local proxy stream, then add to catalog under opencode provider.

## Upstream verification (direct, with browser UA)
Direct upstream responses for the user's OpenCode Zen key: deepseek-v4-pro and deepseek-v4-flash return 401 CreditsError "No payment method" (paid models need billing set up on user's OpenCode workspace), deepseek-v4-flash-free returns 429 FreeUsageLimitError (Console rate limit — transient). So ALL THREE deepseek models exist on OpenCode Zen and the API accepts the user's key (the earlier "key rejected" message was a wrong mapping of upstream 401 CreditsError). Note: my server's friendly mapping labeled upstream 401 as "key rejected" — should map CreditsError to a billing-needed message instead (minor UX fix). The deepseek-v4-flash-free (20-30 req/min free tier per OpenCode docs) is the free one from the reel.

## Decision for Batch 76
Add 3 models under the opencode provider: DeepSeek V4 Pro (paid, needs billing), DeepSeek V4 Flash (paid, needs billing), DeepSeek V4 Flash Free (free tier, rate limited). Vision: none (text only). Mark V4 Flash Free prominently (free badge). Catalog ids: deepseek-v4-pro, deepseek-v4-flash, deepseek-v4-flash-free. Improve friendly error: upstream CreditsError 401 → "this model needs a payment method on the provider — try a free model instead" instead of "key rejected".

## Batch 76 implementation state
Added to src/providers.ts: opencode/deepseek-v4-pro ("DeepSeek V4 Pro"), opencode/deepseek-v4-flash ("DeepSeek V4 Flash"); deepseek-v4-flash-free already existed in catalog. MODELS now = 22.
Server upstream error JSON is forwarded as-is (res.json(j)) when upstream ok=false — so CreditsError message already reaches client via the client's SSE/JSON path. The client shows provider error bubbles; the raw "No payment method. Add a payment method here: ..." is understandable enough. No change needed on server.
Next: update tests/providers.test.ts expect(MODELS.length).toBe(22), typecheck, pnpm test, checkpoint, sync via scripts/sanitize-for-github.py + scripts/sync-full-via-api.py (sanitize script itself is EXCLUDED in sync), then deliver.
Live site still serves OLD build — remind user to press Publish.
