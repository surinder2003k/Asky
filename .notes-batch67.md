# Batch 67 state notes (2026-08-18)

## User feedback (screenshots)
1. Composer textarea grew too tall with long text, hiding the chat on mobile. FIXED: both composers (home ~line 717, in-chat ~line 1095) and edit composer (~line 1460) now `max-h-[7.2em] overflow-y-auto` — grows up to ~4 lines, then internal scroll.
2. Web search hijacked normal chat: "Hi" / "create a simple HTML table" sent → AI answered with "I searched the web…" pasted into the user message bubble, hiding the user's real text. FIXED below.

## Web search fix (ChatGPT-style)
- `src/storage.ts`: webSearch default changed `true → false`.
- `src/components/ChatScreen.tsx` send():
  - `wantSearch = settings.webSearch === true` (strict, default OFF).
  - Search results no longer merged into stored user message: `userMsg.content = text.trim()` (pure user text).
  - `searchContext` (SEARCH_CONTEXT_PREFIX + results + "Answer the question below using the above context:\n") is sent as HIDDEN context appended to the system message (`hiddenContext = systemText ? ... : searchContext`) → goes to AI only, never visible in user bubble.
  - `sources` on assistant message still stored → citation chips under answer still work.
- SettingsModal toggle at line ~347 still `checked={settings.webSearch !== false}` — fine (shows state correctly; default now off).
- Per-chat toggle: NOT added yet — user asked ChatGPT-style; keep simple (global toggle in Settings only).

## Remaining before checkpoint
- [ ] tsc + tests pass
- [ ] screenshot verify mobile
- [ ] checkpoint (Batch 67)
- [ ] sync to GitHub website branch: `cd /home/ubuntu/ai_chat_app && python3 scripts/sync-via-api.py` (GITHUB_PUSH_TOKEN in env; previous remote tip 3ac89d4)
- [ ] deliver to user in Hinglish, link: https://aichatapp-8ksusdph.manus.space
- [ ] mark todo.md items done
