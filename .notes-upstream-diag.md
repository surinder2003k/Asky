# Upstream Nvidia streaming diagnostics (2026-08-19)

## Confirmed fixed via proxy (model merge bug)
- server/index.ts + server/aiProxy.ts now merge `model: modelId` into upstream body.
- Via proxy :3001: meta/llama-3.1-8b-instruct → HTTP 200, stream completes, [DONE] seen. BUG FIXED.
- minimaxai/minimax-m3 → 400 "DEGRADED function cannot be invoked" — Nvidia-side model degradation (DEGRADED state for that function id), not our bug.

## Upstream streaming quirks (important)
1. Some Nvidia models (meta/llama-3.2-3b-instruct) return 200 with stream but NEVER send [DONE] and keep connection open until timeout (python: read timed out after 4.8s+; curl: HTTP:000). Client/server must handle missing [DONE]:
   - Our server pump already has 30s idle watchdog (res.end on stall) + res.on("close").
   - BUT idle watchdog only fires after 30s of silence; llama-3.2 kept streaming chunks without [DONE]... actually it timed out mid-stream (no data for 55s?). Either way the missing-[DONE] case must close cleanly.
2. Some models return 404 (google/gemma-3-4b-it via proxy — upstream 404) — model retired from Nvidia catalog.
3. minimax-m3 = DEGRADED → retry on another Nvidia model.

## Fix needed for missing-[DONE]
- When upstream SSE stream ends (reader done) without final [DONE] in data, our server pump currently does res.end() — good, client must treat stream-end as completion. VERIFY src/ai.ts handles stream-end without [DONE] as done (it does: loop exits, onDone fires, started guard).
- Also server-side: add [DONE] injection only if not already appended? Current server/index.ts DOES res.write("data: [DONE]\n\n") after pump — but ONLY in the NON-aiProxy path (server/index.ts has its own /api/chat route; aiProxy.ts relay does NOT inject [DONE]). Two parallel routes exist! Need to check which route the client hits (client calls /api/chat → could go to either depending on server entry). server/index.ts is what tsx watch runs (dev:server = tsx watch server/index.ts on :3001). aiProxy.ts appears UNUSED in current app (server/index.ts defines its own /api/chat). → aiProxy.ts edits were redundant.
- Server/index.ts /api/chat: after pump loop, writes explicit "data: [DONE]\n\n" — good for clients expecting [DONE].
- llama-3.2 case: upstream kept connection, no data for ~50s → server idle watchdog (30s) should end it. Watchdog is in aiProxy.ts ONLY. server/index.ts has NO idle watchdog during SSE pump → hanging stream (llama-3.2) → cursor stuck forever! ADD idle watchdog + res.on close to server/index.ts pump too.

## Client side: /api/chat → server/index.ts route (dev). aiProxy.ts not wired. Confirm no import of aiProxy in server/index.ts (grep showed none).

## Next actions
1. Add 30s idle watchdog + res.on("close") reader.cancel to server/index.ts SSE pump.
2. Remove/keep aiProxy.ts as dead code (harmless). Decide: keep file (may be used later) — leave.
3. Mark Nvidia models in client that are DEAD (404/DEGRADED) for the model list? Not necessary — provider errors surface as friendly bubbles. But MiniMax M3 selection caused user's stuck cursor: with watchdog it will now end after 30s with friendly retry notice. Better: mark minimaxai/minimax-m3 as degraded in src/providers.ts with a warning, or keep.
4. Retest llama-3.2 via proxy → should end after watchdog (cursor stops blinking, onDone/retry path).
5. Test mistral/groq/openrouter/opencode_zen/gemini via proxy.
6. Update todo.md, checkpoint, sync via scripts/sync-full-via-api.py, deliver.

## Keys (for tests)
- nvidia NVAPI_PLACEHOLDER
- mistral MISTRAL_PLACEHOLDER
- groq GROQ_PLACEHOLDER
- openrouter OPENROUTER_PLACEHOLDER
- opencode_zen OPENCODE_PLACEHOLDER_2 (user gave this one earlier; notes also list sk-1Ulnc... — use the one user most recently confirmed)
