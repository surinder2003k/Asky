# Batch 75 diagnosis (2026-08-19)

## User-reported issues (live site screenshots)
1. Model picker panel cut off at LEFT edge on mobile (screenshot: panel shifted left, first chars of model labels "GLM 5.2 / GPT-OSS / MiniMax M3 / Llama 3.3 70B" cut behind sidebar/left viewport).
2. Settings modal on mobile shows its own separate scrollbar (width issue?) — screenshot shows modal with independent scrollbar, should be full-height mobile sheet style.
3. "Couldn't get a reply" on live site with MiniMax M3 (user). Investigated below.

## Backend findings
- Live site aichatapp-8ksusdph.manus.space still serves OLD server code ("Unknown provider" for providerKey=nvidia) — deploy not yet pushed. The latest checkpoint 8d246e70 must be PUBLISHED by user via Publish button for live site to get Batch 74 fixes. NOTE: live = publish state; we can't publish from sandbox — remind user.
- LOCAL dev probe results (server/index.ts on :3001, latest code):
  - minimaxai/minimax-m3: 400 no detail via proxy; DIRECT upstream = RemoteDisconnected (connection aborted, NO response) → Nvidia side issue for this model right now.
  - meta/llama-3.3-70b-instruct: same, upstream closes connection without response (was working in Batch 74!). Likely transient Nvidia outage / maintenance.
  - openai/gpt-oss-20b: 200 OK (2.1s) direct upstream.
  - z-ai/glm-5.2: 200 OK (2.6s) direct upstream.
- curl against integrate.api.nvidia.com also shows status 000 (TLS connect hang) for those models — same upstream issue.
- CONCLUSION: user's "Couldn't get a reply" with MiniMax M3 = Nvidia upstream temporarily down for minimax-m3 + llama-3.3-70b (Batch 74 watchdog handles hangs client-side; the 400 "Nvidia: provider error (400)." has empty detail — should improve error detail propagation: when upstream 400 body empty, say "The model is unavailable on Nvidia right now — try another model").

## Actions
- [ ] Improve makeFriendlyError for empty-detail 400/5xx + 0-byte upstream responses → friendly "model unavailable / try another model" message
- [ ] Client: ai.ts treat empty text upstream error specially (keep model status tracker record)
- [ ] UI: model picker left cut-off fix on mobile (check .model-picker-panel positioning — user screenshot shows panel left edge at x≈0 with rows cut; panel uses left-1/2 -translate-x-1/2? On mobile maybe panel positioned relative to chip near left edge and overflows viewport). Fix: constrain panel max-width within viewport, add min-margin (e.g., max-w-[calc(100dvw-16px)]).
- [ ] UI: Settings modal mobile scrollbar — check SettingsModal container styling (height overflow). Make it max-h-[calc(100dvh-32px)] overflow-y-auto with padding, scrollbar-width none.
- [ ] Test all providers E2E through dev proxy after fixes, then checkpoint + deliver (remind Publish for live).

## Status
- groq: 401 on live earlier (old code path) — new code verified groq 200 locally in Batch 74.
- mistral/openrouter/opencode verified OK in Batch 74 via proxy.

## Progress update (after fixes applied)
- DONE: Model picker repositioned with JS anchoring (chipRef getBoundingClientRect, clamp within viewport, 12px margin; uses `fixed` positioning, panelPos state). File: src/components/ChatScreen.tsx ModelChip (~line 1190-1280). Also chipRef added. tsc clean.
- DONE: Settings modal full-height mobile sheet: outer overlay no longer overflow-y-auto; modal container = flex h-[100dvh] w-full flex-col on mobile (sm: unchanged); inner content = min-h-0 flex-1 overflow-y-auto. tsc clean.
- Still to do:
  - [ ] Friendly error for upstream 400 with empty detail / connection-aborted (makeFriendlyError in server/index.ts + maybe ai.ts client fallback): "The model is unavailable on Nvidia right now — try another model."
  - [ ] Verify picker panel renders under chip via JS — screenshot tool can't click, but screenshot of home OK. Consider JS-based open via useEffect placement already covers.
  - [ ] Run test suite (pnpm test) + typecheck; checkpoint.
  - [ ] GitHub sync (scripts/sync-full-via-api.py; remember sanitize keys script scripts/sanitize-for-github.py runs before sync: python3 scripts/sanitize-for-github.py --sanitize then sync then python3 scripts/sanitize-for-github.py --restore).
  - [ ] Deliver: tell user live site (aichatapp-8ksusdph.manus.space) still serves OLD published build ("Unknown provider" errors) until they press Publish button; the preview shows latest. Also explain minimax-m3 + llama-3.3-70b currently failing on Nvidia upstream directly (connection closed, 0-byte) — transient provider outage, suggest GLM 5.2 / GPT-OSS 20B (both verified 200 OK).
- Dev proxy E2E note: /api/chat takes {providerKey, modelId, apiKey, body:{messages,stream}}. Live site old server at :3000 unreachable locally.
- Nvidia probe results: minimax-m3 FAIL (RemoteDisconnected), llama-3.3-70b FAIL (same), gpt-oss-20b OK, glm-5.2 OK.
- tsc watch errors shown are stale debug-groq logs from 05:54 (resolved in Batch 74, no code issue).
