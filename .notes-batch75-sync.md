# Batch 75 sync notes (2026-08-19)

## Done (code)
- Batch 75 checkpoint SAVED: version bdfa7669 (also deployed: aichatapp-8ksusdph.manus.space checkpoint auto-publishes)
- todo.md batch 75 items all marked [x]

## Sync failure debug — ROOT CAUSE FOUND
- d4f8ce9... was a COMMIT sha, not a tree sha! Last batch's sync script must have stored the commit sha in LAST_SYNC_TREE.
- Actual tree sha of that commit: 4b68187496cf0c2c4e2033acaf0806d1eab9a8a7 (parent commit "Batch 70 sync", tip = d4f8ce9).
- Fixed: LAST_SYNC_TREE in sync-full-via-api.py now = 4b68187496cf0c2c4e2033acaf0806d1eab9a8a7. Rerun sync.
- If it still 422s: src/websearch.ts deletion entry — check whether websearch.ts EXISTS in the base tree (it was deleted in batch 70, so sha:null delete entry on a non-existent path may trigger BadObjectState). Remove the DELETED entry if absent.
- After successful sync: update LAST_SYNC_TREE to the NEW tree sha (from /git/trees response), not the commit sha.
- Also restore real key files if sanitized files are still in place: /tmp/asky-key-backups has originals (python3 scripts/sanitize-for-github.py has a restore mode? check its CLI).
- 422 on GitHub blob POST: secret scanning blocks `scripts/sanitize-for-github.py` itself (contains real keys as backup/restore originals).
- Added EXCLUDED = {"scripts/sanitize-for-github.py"} to sync-full-via-api.py + pop from local tree.
- STILL failing with 422 after exclusion — need to identify WHICH file now fails (run /tmp/debug422.py style scan but excluding sanitize file; OR check: the sync script itself was also part of 'to_post' previously since it changed — after exclusion it should skip; maybe another file now contains a secret? The sanitize script rewrites 9 files with placeholders; those sanitized copies are what sync posts — fine.
- Note: sanitize-for-github.py backs up originals to /tmp/asky-key-backups; originals are restored after sync. Currently sanitized versions may still be in place if previous run restored — CHECK: run `python3 scripts/sanitize-for-github.py restore` equivalent if the sync script has a restore mode; earlier batch 74 flow: sanitize → sync → restore.

## Next steps
1. Re-run debug422 scan (exclude sanitize-for-github.py) to find the failing file.
2. Fix, rerun sync-full-via-api.py.
3. Update LAST_SYNC_TREE in sync script to new tree sha.
4. Verify website branch README + latest server code present (curl api.github.com repos/surinder2003k/Asky/git/trees/NEW_SHA).
5. Deliver: live link https://aichatapp-8ksusdph.manus.space (checkpoint bdfa7669 auto-deployed).

## Key facts
- Repo: surinder2003k/Asky, branch website. Auth: env GITHUB_PAT (or GITHUB_PUSH_TOKEN), Authorization: Bearer.
- Sync script: scripts/sync-full-via-api.py (Git Data API: blobs, trees, commits, ref).
- Live site serves PUBLISHED checkpoint build (user must click Publish for old site update; this checkpoint auto-deployed per deploy notify).
