# Batch 72 notes — model picker mobile fixes (2026-08-19)

## User problem (screenshot /home/ubuntu/upload/Screenshot_20260819-103139.Chrome.png)
On mobile: tapping model chip opens the picker. Two issues:
1. The "Search models..." input opens the phone software keyboard when the picker opens (autoFocus). Bad UX — typing should work without keyboard OR keyboard should be usable; actual complaint: "text keyword open kr raha" (typing opens keyboard) AND list scroll still stuck.
2. List scroll inside picker still not working on touch.

## Current code structure (ChatScreen.tsx)
- ModelChip component at line 1165. State: filter (1182), favsOpen (1183). pick() at 1184 records usage via recordModelUsed.
- Panel: line 1221-1224: `div.model-picker-panel absolute left-0 top-full z-50 ...` with `style maxHeight: "min(480px, calc(100dvh - 120px))"`. Backdrop: fixed inset-0 z-40 at line 1220 calls setOpen(false).
- Single scroll container at line 1226: `div.flex.flex-col.touch-pan-y.overflow-y-auto.overscroll-contain.px-1.5.py-1.5` style maxHeight 100%.
- Search input at 1230-1236: autoFocus, filters on onChange.
- CSS index.css lines 89-97: .model-picker-panel overflow-x-hidden overflow-y-auto (line ~92-94) + inner div rule.

## Planned fix (Batch 72)
1. Full-screen modal overlay approach on mobile (and desktop too for consistency):
   - Backdrop fixed inset-0 z-40 already exists; make the panel a centered fixed card on small screens OR keep anchored but ensure backdrop has `overflow-hidden` on body while open.
   - Add body scroll lock while open: useEffect with document.body.style.overflow='hidden' + restore on close (also for touch scroll trap — must lock page, then the inner container scrolls).
2. Remove autoFocus from the search input → keyboard won't pop automatically. Typing still works if user explicitly taps the field (acceptable; ChatGPT-like). Also add inputMode/text handling. Consider preventing keyboard entirely with a hidden key-capture div? Simpler: drop autoFocus and add onFocus prevention? Best: keep input but NO autoFocus; on desktop autofocus is fine but mobile keyboard is the issue — drop autoFocus unconditionally.
3. Ensure scroll container: `overflow-y-auto touch-pan-y -webkit-overflow-scrolling` on single div with explicit height (e.g., `height: calc(min(480px, 100dvh - 120px) - 44px)` style) rather than max-height 100%.
4. index.css: change .model-picker-panel from `overflow-y-auto` to `overflow:visible` (panel must not scroll; only inner div). Line ~92-94 has overflow-x-hidden overflow-y-auto — must remove overflow-y-auto.
5. Body lock: add useEffect inside ModelChip (only when open): save prev overflow, set hidden; cleanup restores.

## Files
- src/components/ChatScreen.tsx (ModelChip lines 1165-1450)
- src/index.css (lines 89-97 picker CSS)

## After fix
- pnpm run check, pnpm test (109 pass expected)
- checkpoint, sync via python3 scripts/sync-full-via-api.py (GITHUB_PUSH_TOKEN in env; website branch)
- Live: https://aichatapp-8ksusdph.manus.space

## Earlier batches in todo.md: Batch 70 web search removed (2e32dd24, README on website 840052d + main dceb214b).
