# Batch 74 state notes (2026-08-19)

## User screenshots / complaints (11:01 & 10:58 phone shots)
1. Picker gray scrollbar visible — must hide (scrollbar-width: none; ::-webkit-scrollbar {display:none}).
2. Picker anchored at corner (absolute left-0 top-full, line 1248 ChatScreen.tsx) — should be centered: `left-1/2 -translate-x-1/2`, and must keep key-status ("key set"/"add key") text visible (user says he can't see key status on the side).
3. User messages appear stuck to left side, not centered column — in screenshot, user bubble sits at extreme left. The messages container (line 876): `mx-auto flex max-w-2xl/3xl gap-5 px-4 py-6`. The inner messages div may have width issue on mobile? Actually flex + mx-auto should center. But user bubble line 1565 `flex justify-end`, max-w-[85%] — fine. Problem may be the scroll container width on mobile with sidebar hidden; or the outer chat wrapper uses w-full and flex so mx-auto does nothing (flex children don't honor margin auto unless justify-center). FIX: add `justify-center` to the flex at line 876 (flex + mx-auto doesn't center in a flex container!).
4. NVIDIA model selected but no response — streaming hangs. Test all providers.

## Key facts
- Home screen mobile screenshot OK: header, hero, model chip, suggestions, composer render fine and centered.
- EADDRINUSE 3001 warnings in logs are old log remnants (Batch 68), server running fine on 3000.
- Picker panel: line 1248 `absolute left-0 top-full z-50 mt-1.5 w-[300px] max-w-[calc(100vw-24px)] ... model-picker-panel`.
- index.css lines 87-100: model-picker-panel scrollbar styling (6px thumb, var(--asky-border)) — user wants hidden scrollbar: change to display none / scrollbar-width none.
- inner touch scroll div: `.model-picker-panel .touch-pan-y` height min(480px, calc(100dvh-120px)).
- Model picker state: body scroll-lock was removed (Batch 73), touch containment via touchmove stop.
- Live: https://aichatapp-8ksusdph.manus.space ; GitHub website branch; sync script: python3 scripts/sync-full-via-api.py (Git Data API, PAT via GITHUB_PAT_11BRJPU3Y... env var `GITHUB_TOKEN`? check script header).
- Last website branch commit: 77a1225. Last checkpoint: 94b83ca6. Tests: 109 pass.

## Remaining plan
1. index.css: hide picker scrollbar (scrollbar-width:none; ::-webkit-scrollbar{display:none}), keep inner div scroll working.
2. ChatScreen line 1248: center picker `left-1/2 -translate-x-1/2`.
3. ChatScreen line 876: add `justify-center` to the flex messages container (fixes side-stuck bubbles).
4. Diagnose NVIDIA streaming: check server/index.ts aiProxy + src/ai.ts per-provider base URLs; test each provider with a small node script or curl against /api/ai (dev server proxy). Known earlier: OpenRouter default "z-ai/..." etc.
5. tests + typecheck, checkpoint, sync, deliver.
